import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Journeys/Checkout/2. Housing Protection — Findings" />

# 2. Housing Protection — Findings

Step 2. Housing Protection opt-in/opt-out.

> **Not yet captured.** Requires completing step 1. Supply a URL or leave for flow capture.
>
> Add its URL to `capture/targets.json` and run `pnpm capture && pnpm audit && pnpm report`.

---

No findings yet — this page has not been captured.
