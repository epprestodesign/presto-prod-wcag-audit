import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Journeys/Checkout/3. Payment Information — Findings" />

# 3. Payment Information — Findings

Step 3. Card entry and billing address. CAPTURED ONLY - never submitted.

> **Not yet captured.** Requires completing steps 1-2. Supply a URL or leave for flow capture.
>
> Add its URL to `capture/targets.json` and run `pnpm capture && pnpm audit && pnpm report`.

---

No findings yet — this page has not been captured.
