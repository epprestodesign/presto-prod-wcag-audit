import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Journeys/Landing/Event Landing — Findings" />

# Event Landing — Findings

Event entry point — hero, event branding, and the initial search form. First page in the journey; everything else is reached from here.

**Captured from:** `https://presto.eventpipe.com/event/9f16e139-935b-48f6-80cf-da2628b33b9c/`

---

## Summary

| Impact | Count |
| --- | --- |
| Critical | 2 |
| Serious | 4 |
| Moderate | 1 |
| Minor | 0 |

7 distinct issues on this page.

---
### 1. 2 form controls have no programmatic label

<span class="f-badge critical">critical</span> <span class="f-badge sc">WCAG 4.1.2, 3.3.2 (Level A)</span>

| | |
| --- | --- |
| **Rule** | `unlabelled-controls` |
| **Detected by** | manual |
| **Occurrences** | 2 |
| **Viewports** | desktop, mobile |


These controls rely on placeholder text or adjacent visual text. A placeholder is not an accessible name: it is not announced by every screen reader, and it disappears the moment the user starts typing, removing the only cue to what the field was for.

**Evidence**

- `<textarea> placeholder="Write your message here"`
- `<input type="file"> placeholder=""`

<details>
<summary>Affected elements (2)</summary>

```html
<!-- div:nth-of-type(3) > div:nth-of-type(1) > div > div:nth-of-type(2) > div:nth-of-type(3) > div:nth-of-type(3) > div > textarea -->
<textarea name="" placeholder="Write your message here">
```

```html
<!-- div:nth-of-type(3) > div:nth-of-type(1) > div > div:nth-of-type(2) > div:nth-of-type(3) > div:nth-of-type(3) > span:nth-of-type(1) > input -->
<input name="file" placeholder="">
```

</details>

#### Fix — Give every form control a real, programmatic label

These controls are identified only by placeholder text or by nearby visual text. A placeholder is not an accessible name: it is not reliably announced, it fails contrast at its default grey, and it disappears the moment the user types — removing the only cue to what the field was for. Users who navigate a form field-by-field with a screen reader hear "edit, blank".

**Before** (production)

```html
<!-- checkout step 1: label is a sibling div, not associated -->
<div class="field-label">Team Name <span class="req">*</span></div>
<input type="text" name="Rooms[0].TeamName" placeholder="i.e. All-Stars 12U Team">
```

**After**

```html
<!-- Associate explicitly. The for/id pair is what makes the text a *name*. -->
<label for="team-name">
  Team Name
  <!-- asterisk hidden from AT; the required attribute already conveys it -->
  <span class="req" aria-hidden="true">*</span>
</label>
<input type="text"
       id="team-name"
       name="Rooms[0].TeamName"
       required
       aria-required="true"
       placeholder="i.e. All-Stars 12U Team">
```

**Vue / Quasar form**

```html
<!-- Vue/Quasar side: q-input renders the association for you.
     Never replace :label with a bare placeholder. -->
<q-input
  v-model="teamName"
  label="Team Name"
  :rules="[val => !!val || 'Team name is required']"
  hint="i.e. All-Stars 12U Team"
/>

<!-- If a design truly forbids a visible label, name it explicitly — but a
     visible label is preferred: it also serves users with cognitive load. -->
<q-input v-model="query" aria-label="Search by property name" />
```

**Notes**

- Production ids here are order-scoped (`Rooms[0].Guests[0].FirstName`). Reuse that exact string as the id so for/id stays unique when a second guest row is added.
- Do not point `for` at a wrapper div — it must reference the control itself.

**Effort:** S &nbsp;•&nbsp; **Owner:** both

---

### 2. Buttons must have discernible text

<span class="f-badge critical">critical</span> <span class="f-badge sc">WCAG 4.1.2 (Level A)</span>

| | |
| --- | --- |
| **Rule** | `button-name` |
| **Detected by** | axe-core |
| **Occurrences** | 1 |
| **Viewports** | mobile |
| **Reference** | [button-name](https://dequeuniversity.com/rules/axe/4.12/button-name?application=axeAPI) |



<details>
<summary>Affected elements (1)</summary>

```html
<!-- div[data-snapshot="1"],div[data-v-8aaf5340=""]:nth-child(2) > .q-btn--flat.q-btn--actionable[type="button"] -->
<button data-v-8aaf5340="" class="q-btn q-btn-item non-selectable no-outline q-btn--flat q-btn--rectangle text-primary q-btn--actionable q-focusable q-hoverable" tabindex="0" type="button">
```

</details>

#### Fix — Name icon-only buttons

These buttons contain only an icon glyph, so their accessible name resolves to the ligature text ("arrow_drop_down", "search") or to nothing. A screen reader announces "button" with no indication of what it does. This is the single largest critical-impact count in the audit, driven by the carousel arrows repeated on every hotel card.

**Before** (production)

```html
<!-- Material icon ligature becomes the accessible name -->
<button class="q-btn">
  <i class="material-icons">arrow_left</i>
</button>
```

**After**

```html
<!-- aria-label names the control; aria-hidden stops the ligature
     text from being announced alongside it. -->
<button class="q-btn" aria-label="Previous photo">
  <i class="material-icons" aria-hidden="true">arrow_left</i>
</button>
```

**Vue / Quasar form**

```html
<!-- Quasar: q-btn with :icon still needs a name. -->
<q-btn icon="arrow_left" aria-label="Previous photo" flat round />

<!-- Better for carousels: name the target, not the direction, so a user
     hearing 40 identical "Previous photo" buttons can tell them apart. -->
<q-btn
  icon="arrow_left"
  :aria-label="`Previous photo of ${hotel.name}`"
  flat round
/>
```

**Notes**

- The carousel arrows repeat once per hotel card — 80 nodes on the desktop search page alone. Fixing the shared card component clears most of the count in one change.
- If the control is genuinely decorative and duplicated by adjacent text, hide it instead: `aria-hidden="true" tabindex="-1"`.

**Effort:** S &nbsp;•&nbsp; **Owner:** presto.eventpipe.com (Vue/Quasar)

---

### 3. 6 focusable elements produce no visible change when focused

<span class="f-badge serious">serious</span> <span class="f-badge sc">WCAG 2.4.7 (Level AA)</span>

| | |
| --- | --- |
| **Rule** | `focus-not-visible` |
| **Detected by** | manual |
| **Occurrences** | 6 |
| **Viewports** | desktop |


Measured by tabbing to each element and diffing the rendered pixels against the same region unfocused. These produced no perceptible change, so a sighted keyboard user cannot tell where they are.

**Evidence**

- `<div> "Close chat" 20x20 — outline: auto 1px rgb(0, 95, 204)`
- `<div> "Close chat" 20x20 — outline: auto 1px rgb(0, 95, 204)`
- `<div> "Close chat" 20x20 — outline: auto 1px rgb(0, 95, 204)`
- `<div> "Close chat" 20x20 — outline: auto 1px rgb(0, 95, 204)`
- `<div> "Close chat" 20x20 — outline: auto 1px rgb(0, 95, 204)`
- `<div> "Close chat" 20x20 — outline: auto 1px rgb(0, 95, 204)`

<details>
<summary>Affected elements (6)</summary>

```html
<!-- div:nth-of-type(3)>div:nth-of-type(2)>div:nth-of-type(3) -->
<div> Close chat
```

```html
<!-- div:nth-of-type(3)>div:nth-of-type(2)>div:nth-of-type(3) -->
<div> Close chat
```

```html
<!-- div:nth-of-type(3)>div:nth-of-type(2)>div:nth-of-type(3) -->
<div> Close chat
```

```html
<!-- div:nth-of-type(3)>div:nth-of-type(2)>div:nth-of-type(3) -->
<div> Close chat
```

```html
<!-- div:nth-of-type(3)>div:nth-of-type(2)>div:nth-of-type(3) -->
<div> Close chat
```

```html
<!-- div:nth-of-type(3)>div:nth-of-type(2)>div:nth-of-type(3) -->
<div> Close chat
```

</details>

#### Fix — Make these specific controls show focus

Measured by tabbing to each control and diffing rendered pixels: these produced no perceptible change when focused. Most are third-party chat-widget controls and 1x1 proxy inputs behind styled Quasar components.

**Before** (production)

```html
<!-- 1x1 proxy input: a real tab stop the user cannot see -->
<input class="q-field__native" style="width:1px;height:1px">
```

**After**

```html
/* Style the styled wrapper when the proxy inside it has focus, so the
   thing the user actually sees reacts. */
.q-field:focus-within .q-field__control {
  outline: 2px solid #14203c;
  outline-offset: 2px;
}
```


**Notes**

- The chat widget is third-party — either configure its theme for a visible ring or raise it with the vendor.
- See "focus-appearance" for the global rule that causes most of this.

**Effort:** S &nbsp;•&nbsp; **Owner:** both

---

### 4. 4 interactive targets are under the 24x24 CSS-pixel minimum

<span class="f-badge serious">serious</span> <span class="f-badge sc">WCAG 2.5.8 (Level AA)</span>

| | |
| --- | --- |
| **Rule** | `small-targets` |
| **Detected by** | manual |
| **Occurrences** | 4 |
| **Viewports** | desktop, mobile |


WCAG 2.2 requires pointer targets be at least 24x24 CSS pixels unless spaced apart or offered another way. Undersized targets are hardest for users with tremor or reduced dexterity, and on touch devices for everyone.

**Evidence**

- `input "Booking Type" — 1x1px`
- `input "Age Divsion" — 1x1px`
- `a "Privacy Policy" — 81x17px`
- `div "Close chat" — 20x20px`

<details>
<summary>Affected elements (4)</summary>

```html
<!-- div:nth-of-type(2) > div > div > div > div > div:nth-of-type(1) > label > div > div:nth-of-type(1) > div:nth-of-type(1) > div:nth-of-type(2) > input -->
<input> Booking Type
```

```html
<!-- div:nth-of-type(2) > div > div > div > div > div:nth-of-type(3) > label > div > div:nth-of-type(1) > div:nth-of-type(1) > div:nth-of-type(2) > input -->
<input> Age Divsion
```

```html
<!-- div:nth-of-type(1) > div > div > main > footer > div > div > a -->
<a> Privacy Policy
```

```html
<!-- div:nth-of-type(3) > div:nth-of-type(2) > div:nth-of-type(3) -->
<div> Close chat
```

</details>

#### Fix — Enlarge undersized targets

Measured geometry shows these focusable elements render below 24x24 CSS pixels.

**Before** (production)

```html
.star { width: 14px; height: 14px; }
```

**After**

```html
.star { width: 14px; height: 14px; padding: 5px; box-sizing: content-box; }
```


**Notes**

- Identical remedy to target-size; this entry is sourced from measured geometry rather than axe.

**Effort:** M &nbsp;•&nbsp; **Owner:** both

---

### 5. Page contains no headings at all

<span class="f-badge serious">serious</span> <span class="f-badge sc">WCAG 1.3.1 (Level A)</span>

| | |
| --- | --- |
| **Rule** | `no-headings` |
| **Detected by** | manual |
| **Occurrences** | 1 |
| **Viewports** | desktop, mobile |


Not one h1-h6 or role="heading" element exists on this page. Screen-reader users navigate primarily by heading (the "H" key / rotor); with no headings there is no way to skim the page or jump to a section, so the only option is to read linearly from the top.

**Evidence**

- `0 heading elements found among 68 interactive elements`


#### Fix — Introduce a real heading outline

The search, hotel detail and lookup pages contain no h1–h6 at all. Heading navigation is the primary way screen-reader users skim a page — the rotor / "H" key. With zero headings there is nothing to navigate by, so reaching the tenth hotel means listening through everything before it.

**Before** (production)

```html
<!-- Hotel name is styled text in a div — visually a heading, semantically nothing -->
<div class="hotel-name text-h6">Spark by Hilton Exton</div>
```

**After**

```html
<!-- Same visual weight, real semantics. Quasar's text-h6 class is
     presentational and works on any element. -->
<h3 class="hotel-name text-h6">Spark by Hilton Exton</h3>
```

**Vue / Quasar form**

```html
<!-- Recommended outline for the search page -->
<h1 class="sr-only">Hotels for 2026 - Fall Continental Cup</h1>

<h2 class="sr-only">Search and filters</h2>
  <h3>Search by Property Name</h3>
  <h3>Amenities</h3>
  <h3>Search Radius</h3>

<h2 class="sr-only">Results — 24 hotels</h2>
  <h3>Spark by Hilton Exton</h3>       <!-- one per result card -->

<!-- .sr-only: available to AT, invisible on screen. -->
<style>
.sr-only {
  position: absolute; width: 1px; height: 1px;
  padding: 0; margin: -1px; overflow: hidden;
  clip: rect(0 0 0 0); white-space: nowrap; border: 0;
}
</style>
```

**Notes**

- Do not choose heading level by how big the text looks — level expresses nesting. Style separately.
- One h1 per page, naming what the page is for.
- Result-card headings should be the hotel name: that is what users search for by rotor.

**Effort:** M &nbsp;•&nbsp; **Owner:** presto.eventpipe.com (Vue/Quasar)

---

### 6. Elements must meet minimum color contrast ratio thresholds

<span class="f-badge serious">serious</span> <span class="f-badge sc">WCAG 1.4.3 (Level AA)</span>

| | |
| --- | --- |
| **Rule** | `color-contrast` |
| **Detected by** | axe-core |
| **Occurrences** | 1 |
| **Viewports** | desktop, mobile |
| **Reference** | [color-contrast](https://dequeuniversity.com/rules/axe/4.12/color-contrast?application=axeAPI) |



<details>
<summary>Affected elements (1)</summary>

```html
<!-- div[data-snapshot="1"],.footer-link -->
<a data-v-fd02e343="" href="https://eventpipe.com/legal/privacy" target="_blank" class="footer-link">Privacy Policy</a>
```

</details>

#### Fix — Raise text contrast to 4.5:1 (3:1 for large text)

The largest single count in the audit. Failing text is mostly grey-on-white secondary copy, placeholder text, and white text on light brand fills. At typical mobile brightness this is the difference between readable and invisible for users with low vision — and for everyone outdoors.

**Before** (production)

```html
/* Placeholder and secondary text below 4.5:1 on white */
.text-grey-6      { color: #9e9e9e; }   /* 2.85:1 on #fff — fails */
input::placeholder{ color: #a8a8a8; }   /* 2.54:1 on #fff — fails */
```

**After**

```html
/* Darkened to clear 4.5:1 on white. Values chosen to stay within the
   existing grey ramp so the visual design is preserved. */
.text-grey-6      { color: #6b6b6b; }   /* 4.62:1 — passes AA  */
input::placeholder{ color: #666666; }   /* 5.05:1 — passes AA  */

/* Large text (>=24px, or >=18.66px bold) may use 3:1 */
.hero-subtitle    { color: #767676; }   /* 4.54:1 — passes at any size */
```


**Notes**

- Check the fix against the real background, not white — several failures sit on the #f9f9fa page fill, which needs slightly darker text than pure white does.
- The white-on-photo hero title cannot be fixed with a colour change alone: add a scrim (e.g. `background: linear-gradient(rgba(0,0,0,.45), rgba(0,0,0,.45))`) so contrast is guaranteed regardless of which event image is uploaded.
- Disabled controls are exempt from 1.4.3, so filter those out before triaging the raw count.

**Effort:** M &nbsp;•&nbsp; **Owner:** both

---

### 7. Missing landmark regions: navigation, banner

<span class="f-badge moderate">moderate</span> <span class="f-badge sc">WCAG 1.3.1 (Level A)</span>

| | |
| --- | --- |
| **Rule** | `missing-landmarks` |
| **Detected by** | manual |
| **Occurrences** | 1 |
| **Viewports** | desktop, mobile |


Landmarks are how assistive-technology users move between regions of a page. Without a &lt;main&gt; landmark there is no way to jump past the header and filters straight to the results.

**Evidence**

- `Landmarks present: main, footer`


#### Fix — Wrap the page in landmark regions

Only a &lt;footer&gt; exists site-wide. Landmarks let assistive-tech users jump between regions — without &lt;main&gt;, there is no way to skip the header, hero and filter rail and land on the results.

**Before** (production)

```html
<div id="app">
  <div class="header">…</div>
  <div class="filters">…</div>
  <div class="results">…</div>
  <footer>…</footer>
</div>
```

**After**

```html
<div id="app">
  <header role="banner">
    <nav aria-label="Primary">…</nav>
  </header>

  <!-- The single most valuable addition: a jump target for the results -->
  <main id="main-content">
    <!-- Two search regions on one page must be told apart by name -->
    <search aria-label="Hotel filters">…</search>
    <section aria-label="Search results">…</section>
  </main>

  <footer role="contentinfo">…</footer>
</div>
```


**Notes**

- Exactly one &lt;main&gt; per page.
- Name repeated landmarks (`aria-label`) — two unnamed &lt;nav&gt;s are indistinguishable in a landmark list.
- &lt;search&gt; is the HTML element for a search region; `role="search"` is the fallback for older AT.

**Effort:** S &nbsp;•&nbsp; **Owner:** both

---
